#ifndef GLOBAL_VARS
#define GLOBAL_VARS

#include <iostream>

// Global type, height, width, intensity, hi, lo, and sig variables
extern char type[10];
extern int height;
extern int width;
extern int intensity;
extern int hi;
extern int lo;
extern double sig;

#endif